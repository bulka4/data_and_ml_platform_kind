# Name your project! Project names should contain only lowercase characters
# and underscores. A good package name should reflect your organization's
# name or the intended use of these models
name: 'data_warehouse'
version: '1.0.0'

# This setting configures which "profile" dbt uses for this project.
profile: 'my_spark_project'

# These configurations specify where dbt should look for different types of files.
# The `model-paths` config, for example, states that models in this project can be
# found in the "models/" directory. You probably won't need to change these!
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

# directories to be removed by `dbt clean`
clean-targets:
  - "target"
  - "dbt_packages"

models:
  data_warehouse:
    # Set up configs for models from the models/dwh_fact folder.
    dwh_fact:
      # Tables will be saved in the schema called {project_schema}_{profiles_schema}, where:
      #   - project_schema - schema name specified in dbt_project.yml file
      #   - profiles_schema - schema name specified in profiles.yml file
      +schema: fact
      # How to save models. It can be 'table' or a 'view'.
      +materialized: table
    # Set up configs for models from the models/dwh_dim folder.
    dwh_dim:
      +schema: dim
      +materialized: table
    dwh_raw:
      +schema: raw
      +materialized: table
    # Folders to create tables which simulates data imported from external sources
    source:
      source1:
        +schema: source1
        +materialized: table
      source2:
        +schema: source2
        +materialized: table